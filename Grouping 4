 For each course section offered in 2009, find the average total credits (tot_cred) of all students enrolled in the section, if the section had at least 2 students.


select avg(tot_credits),course_id
from student,takes
where takes.ID = student.ID and year = 2009
group by course_id
having count(ID) >= 2;eita amr
          OR
 select course_id, semester, year, sec_id, avg (tot_cred)
 from takes natural join student
 where year = 2009
 group by course_id, semester, year, sec_id
 having count (ID) >= 2;
